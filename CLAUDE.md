# ScoreTracker

## After each change

1. **Deploy** — use the MCP deployer tool: `mcp__deployer__deploy` with `project_path: /home/vinden/projects/scoretracker`
2. **Bump cache version** — increment `?v=YYYYMMDD` on the `style.css` and `js/main.js` references in `web/index.html`
3. **Commit** — `git add` changed files, `git commit`
4. **Push** — `git push`
